2026-03-25

- Reviewed the current V1 state and confirmed the product loop is already usable: site -> bookshelf -> built-in book -> reader -> local resume.
- Recorded the next focus as UX stabilization rather than backend expansion.
- Captured the user's requested next-step improvements:
  - one-click Chinese / Japanese / English UI switching
  - collapsible reader volume selector instead of a permanently floating top overlay
  - always-reachable settings / chapter / locate / search / bookshelf actions
  - explicit safe return-to-bookshelf flow with saved-progress clarity
  - responsive audit for control position, size, and spacing
  - cleanup of remaining garbled text and inconsistent UI copy
- Added a dedicated backlog file `V1_UX_UPGRADE_BACKLOG.txt` to serve as the handoff for the next coding session.
- Implemented the UX upgrade pass the same night:
  - added persistent tri-language UI switching
  - localized the main site pages, bookshelf, reader controls, entry list, settings modal, and chapters modal
  - converted the reader's fixed top overlay into a compact toolbar with a collapsible volume panel
  - added always-available settings / chapters / volume / bookshelf actions in the bottom control area
  - clarified that reader progress is auto-saved locally and safe to keep when returning to the bookshelf
- Verified the new UX pass with `npm run lint` and `npm run test:local`.
